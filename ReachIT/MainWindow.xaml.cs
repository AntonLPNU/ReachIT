using System.Windows;

namespace ReachIT
{
    // Hosts the main workspace window shell.
    public partial class MainWindow : Window
    {
        public MainWindow()
        {
            InitializeComponent();
        }
    }
}