<Window x:Class="ReachIT.QuickAddTaskWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        Title="{DynamicResource QuickAdd.Title}"
        Icon="Resources/AppIcon.ico"
        Width="460"
        Height="560"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        Topmost="True"
        ShowInTaskbar="False"
        Loaded="Window_Loaded">
    <Window.InputBindings>
        <KeyBinding Key="Enter" Command="{Binding SaveCommand}" />
        <KeyBinding Key="Escape" Command="{Binding CancelCommand}" />
    </Window.InputBindings>

    <Border Style="{StaticResource ReachItCardStyle}" Padding="18" CornerRadius="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Cursor="SizeAll"
                  MouseLeftButtonDown="TitleArea_MouseLeftButtonDown"
                  ToolTip="Drag to move this window.">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="ClipboardPlus" Width="24" Height="24" Foreground="{StaticResource NeonPinkBrush}" Margin="0,3,8,0" />
                        <TextBlock Text="{DynamicResource QuickAdd.Title}" Style="{StaticResource ReachItPageTitleTextStyle}" FontSize="24" />
                    </StackPanel>
                    <TextBlock Text="{DynamicResource QuickAdd.Subtitle}" Style="{StaticResource ReachItMutedTextStyle}" Margin="0,4,0,14" />
                </StackPanel>
                <TextBlock Grid.Column="1"
                           Text="Esc"
                           Style="{StaticResource ReachItMutedTextStyle}"
                           VerticalAlignment="Top"
                           Margin="10,6,0,0" />
            </Grid>

            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Text="{DynamicResource QuickAdd.TaskTitle}" Style="{StaticResource ReachItLabelTextStyle}" />
                <TextBox x:Name="TitleBox" Grid.Row="0" Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" Margin="0,22,0,10" />

                <TextBlock Grid.Row="1" Text="{DynamicResource QuickAdd.Description}" Style="{StaticResource ReachItLabelTextStyle}" />
                <TextBox Grid.Row="1" Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="80" Margin="0,22,0,10" />

                <TextBlock Grid.Row="2" Text="{DynamicResource QuickAdd.Project}" Style="{StaticResource ReachItLabelTextStyle}" />
                <ComboBox Grid.Row="2" ItemsSource="{Binding Projects}" SelectedItem="{Binding SelectedProject}" DisplayMemberPath="ProjectName" Margin="0,22,0,10" />

                <TextBlock Grid.Row="3" Text="{DynamicResource QuickAdd.Parent}" Style="{StaticResource ReachItLabelTextStyle}" />
                <TextBox Grid.Row="3" Text="{Binding ParentOrGroup, UpdateSourceTrigger=PropertyChanged}" Margin="0,22,0,10" />

                <Grid Grid.Row="4" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="12" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="{DynamicResource QuickAdd.Priority}" Style="{StaticResource ReachItLabelTextStyle}" />
                        <ComboBox ItemsSource="{Binding Priorities}" SelectedItem="{Binding Priority}" Margin="0,4,0,0" />
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="{DynamicResource QuickAdd.Deadline}" Style="{StaticResource ReachItLabelTextStyle}" />
                        <DatePicker SelectedDate="{Binding DeadlineDate}" Margin="0,4,0,0" />
                    </StackPanel>
                </Grid>

                <TextBlock Grid.Row="5" Text="{DynamicResource QuickAdd.Tags}" Style="{StaticResource ReachItLabelTextStyle}" />
                <TextBox Grid.Row="5" Text="{Binding Tags, UpdateSourceTrigger=PropertyChanged}" Margin="0,22,0,10" ToolTip="Comma separated tags." />

                <TextBlock Grid.Row="6" Text="{Binding StatusText}" Foreground="{StaticResource NeonPinkBrush}" />
            </Grid>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
                <Button Command="{Binding SaveCommand}" Width="118">
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="ContentSave" Width="16" Height="16" Margin="0,0,8,0" />
                        <TextBlock Text="{DynamicResource QuickAdd.Save}" />
                    </StackPanel>
                </Button>
                <Button Command="{Binding CancelCommand}" Style="{StaticResource ReachItSecondaryButtonStyle}" Width="118">
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="CloseCircle" Width="16" Height="16" Margin="0,0,8,0" />
                        <TextBlock Text="{DynamicResource QuickAdd.Cancel}" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
