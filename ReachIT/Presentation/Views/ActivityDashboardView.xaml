<UserControl x:Class="ReachIT.Presentation.Views.ActivityDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="0,0,0,16">
            <DockPanel Margin="0,0,0,14">
                <TextBlock Text="{DynamicResource Activity.Title}" Style="{StaticResource ReachItPageTitleTextStyle}" />
                <WrapPanel DockPanel.Dock="Right">
                    <Button Command="{Binding RefreshCommand}" Style="{StaticResource ReachItSecondaryButtonStyle}">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="Refresh" Width="16" Height="16" Margin="0,0,8,0" />
                            <TextBlock Text="{DynamicResource Activity.Refresh}" />
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding PauseTrackingCommand}" Style="{StaticResource ReachItSecondaryButtonStyle}">
                        <TextBlock Text="{DynamicResource Activity.Pause}" />
                    </Button>
                    <Button Command="{Binding ResumeTrackingCommand}">
                        <TextBlock Text="{DynamicResource Activity.Resume}" />
                    </Button>
                </WrapPanel>
            </DockPanel>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.3*" />
                    <ColumnDefinition Width="14" />
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource ReachItCardStyle}">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Activity.NowWorkingOn}" Style="{StaticResource ReachItSectionTitleTextStyle}" />
                        <TextBlock Text="{Binding NowWorkingOn}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource ReachItTitleBrush}" />
                        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                            <TextBlock Text="{DynamicResource Activity.ActiveApp}" Style="{StaticResource ReachItLabelTextStyle}" Margin="0,0,6,0" />
                            <TextBlock Text="{Binding ActiveApp}" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                            <TextBlock Text="{DynamicResource Activity.TaskGuess}" Style="{StaticResource ReachItLabelTextStyle}" Margin="0,0,6,0" />
                            <TextBlock Text="{Binding CurrentTaskGuess}" />
                        </StackPanel>
                        <TextBlock Text="{Binding ContextReason}" Style="{StaticResource ReachItMutedTextStyle}" Margin="0,10,0,0" />
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Style="{StaticResource ReachItCardStyle}">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Activity.ProductivityScore}" Style="{StaticResource ReachItSectionTitleTextStyle}" />
                        <TextBlock Text="{Binding ProductivityScore}" FontSize="34" FontWeight="Bold" Foreground="{StaticResource SkyAquaBrush}" />
                        <ProgressBar Value="{Binding ProductivityScore, Mode=OneWay}" Maximum="100" Margin="0,8,0,12" />
                        <TextBlock Text="{Binding ProductivityExplanation}" Style="{StaticResource ReachItMutedTextStyle}" />
                    </StackPanel>
                </Border>
            </Grid>

            <UniformGrid Columns="4" Margin="0,14,0,0">
                <Border Style="{StaticResource ReachItCardStyle}" Margin="0,0,10,0">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Activity.FilesChanged}" Style="{StaticResource ReachItLabelTextStyle}" />
                        <TextBlock Text="{Binding FilesChangedToday}" FontSize="24" FontWeight="Bold" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource ReachItCardStyle}" Margin="0,0,10,0">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Activity.FocusTime}" Style="{StaticResource ReachItLabelTextStyle}" />
                        <TextBlock Text="{Binding FocusMinutesToday, StringFormat={}{0:F0}m}" FontSize="24" FontWeight="Bold" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource ReachItCardStyle}" Margin="0,0,10,0">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Activity.Interruptions}" Style="{StaticResource ReachItLabelTextStyle}" />
                        <TextBlock Text="{Binding InterruptionsToday}" FontSize="24" FontWeight="Bold" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource ReachItCardStyle}">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Activity.RecentFiles}" Style="{StaticResource ReachItLabelTextStyle}" />
                        <ItemsControl ItemsSource="{Binding RecentFiles}" />
                    </StackPanel>
                </Border>
            </UniformGrid>

            <Grid Margin="0,14,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="14" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Style="{StaticResource ReachItCardStyle}">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Activity.SuggestedLinks}" Style="{StaticResource ReachItSectionTitleTextStyle}" />
                        <ItemsControl ItemsSource="{Binding SuggestedTaskLinks}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding SuggestedTitle}" Margin="0,0,0,6" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
                <Border Grid.Column="2" Style="{StaticResource ReachItCardStyle}">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Activity.SuggestedDone}" Style="{StaticResource ReachItSectionTitleTextStyle}" />
                        <ItemsControl ItemsSource="{Binding SuggestedCompletedTasks}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding SuggestedTitle}" Margin="0,0,0,6" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </Grid>

            <Border Style="{StaticResource ReachItCardStyle}" Margin="0,14,0,0">
                <StackPanel>
                    <DockPanel>
                        <TextBlock Text="{DynamicResource Activity.Timeline}" Style="{StaticResource ReachItSectionTitleTextStyle}" />
                        <Button DockPanel.Dock="Right" Command="{Binding ClearHistoryCommand}" Style="{StaticResource ReachItDangerButtonStyle}" Content="{DynamicResource Activity.ClearHistory}" />
                    </DockPanel>
                    <DataGrid ItemsSource="{Binding Timeline}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" MaxHeight="320">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="{DynamicResource Activity.Time}" Binding="{Binding Time}" Width="80" />
                            <DataGridTextColumn Header="{DynamicResource Activity.Event}" Binding="{Binding EventType}" Width="150" />
                            <DataGridTextColumn Header="{DynamicResource Activity.Subject}" Binding="{Binding Subject}" Width="220" />
                            <DataGridTextColumn Header="{DynamicResource Activity.Detail}" Binding="{Binding Detail}" Width="*" />
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
