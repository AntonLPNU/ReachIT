<!-- Planning placeholder content. -->
<UserControl x:Class="ReachIT.Presentation.Views.PlanningView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="16">
            <StackPanel>
                <TextBlock Text="Day Planner" Style="{StaticResource ReachItPageTitleTextStyle}" Margin="0,0,0,16" />
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="12" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="12" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Style="{StaticResource ReachItCardStyle}">
                        <StackPanel>
                            <TextBlock Text="Due Today / Overdue" Style="{StaticResource ReachItSectionTitleTextStyle}" />
                            <ItemsControl ItemsSource="{Binding TodayTasks}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border BorderBrush="{StaticResource ReachItBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="10" Margin="0,0,0,8" Background="#111827">
                                            <StackPanel>
                                                <TextBlock Text="{Binding Title}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                                <TextBlock Text="{Binding DueDateUtc, StringFormat='{}{0:g}'}" FontSize="11" Style="{StaticResource ReachItMutedTextStyle}" Margin="0,4,0,0" />
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <TextBlock Text="No tasks pending for today. You rock!" Style="{StaticResource ReachItMutedTextStyle}" FontStyle="Italic"
                                       Visibility="{Binding TodayTasks.Count, FallbackValue=Collapsed}" />
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="2" Style="{StaticResource ReachItCardStyle}">
                        <StackPanel>
                            <TextBlock Text="Upcoming" Style="{StaticResource ReachItSectionTitleTextStyle}" />
                            <ItemsControl ItemsSource="{Binding UpcomingTasks}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border BorderBrush="{StaticResource ReachItBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="10" Margin="0,0,0,8" Background="#111827">
                                            <StackPanel>
                                                <TextBlock Text="{Binding Title}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                                <TextBlock Text="{Binding DueDateUtc, StringFormat='{}{0:d}'}" FontSize="11" Style="{StaticResource ReachItMutedTextStyle}" Margin="0,4,0,0" />
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="4" Style="{StaticResource ReachItCardStyle}">
                        <StackPanel>
                            <TextBlock Text="Someday / Backlog" Style="{StaticResource ReachItSectionTitleTextStyle}" />
                            <ItemsControl ItemsSource="{Binding SomedayTasks}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border BorderBrush="{StaticResource ReachItBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="10" Margin="0,0,0,8" Background="#111827">
                                            <StackPanel>
                                                <TextBlock Text="{Binding Title}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                                <TextBlock Text="{Binding Status}" FontSize="11" Style="{StaticResource ReachItMutedTextStyle}" Margin="0,4,0,0" />
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </Grid>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>
