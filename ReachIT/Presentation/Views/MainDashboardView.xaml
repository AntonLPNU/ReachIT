<UserControl x:Class="ReachIT.Presentation.Views.MainDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="0,0,0,16">
            <TextBlock Text="{DynamicResource Dashboard.ControlCenter}" Style="{StaticResource ReachItPageTitleTextStyle}" Margin="0,0,0,14" />

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="14" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="14" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Grid.Column="0" Style="{StaticResource ReachItCardStyle}">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Dashboard.ProjectOverview}" Style="{StaticResource ReachItSectionTitleTextStyle}" />
                        <TextBlock Text="{Binding ProjectName}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource ReachItTitleBrush}" />
                        <TextBlock Text="{Binding ProjectDescription}" Style="{StaticResource ReachItMutedTextStyle}" Margin="0,6,0,0" />
                        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                            <TextBlock Text="{DynamicResource Dashboard.Path}" Style="{StaticResource ReachItMutedTextStyle}" Margin="0,0,4,0" />
                            <TextBlock Text="{Binding ProjectPath}" Style="{StaticResource ReachItMutedTextStyle}" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                            <TextBlock Text="{DynamicResource Dashboard.Progress}" Style="{StaticResource ReachItLabelTextStyle}" Margin="0,0,4,0" />
                            <TextBlock Text="{Binding OverallProgress, StringFormat={}{0:F1}%}" Style="{StaticResource ReachItLabelTextStyle}" />
                        </StackPanel>
                        <ProgressBar Minimum="0" Maximum="100" Value="{Binding OverallProgress, Mode=OneWay}" Margin="0,8,0,0" />
                        <Button Command="{Binding OpenSidePanelCommand}" Style="{StaticResource ReachItSecondaryButtonStyle}" Margin="0,14,0,0" ToolTip="{DynamicResource Explorer.DockTip}">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <iconPacks:PackIconMaterial Kind="FolderOpen" Width="16" Height="16" Margin="0,0,8,0" />
                                <TextBlock Text="{DynamicResource Dashboard.ProjectExplorer}" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Border>

                <Border Grid.Row="0" Grid.Column="2" Style="{StaticResource ReachItCardStyle}">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Dashboard.TodayTasks}" Style="{StaticResource ReachItSectionTitleTextStyle}" />
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{DynamicResource Dashboard.Total}" Margin="0,0,4,0" />
                            <TextBlock Text="{Binding TotalTasks}" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                            <TextBlock Text="{DynamicResource Dashboard.Active}" Margin="0,0,4,0" />
                            <TextBlock Text="{Binding ActiveTasks}" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                            <TextBlock Text="{DynamicResource Dashboard.Completed}" Margin="0,0,4,0" />
                            <TextBlock Text="{Binding CompletedTasks}" />
                        </StackPanel>
                        <TextBlock Text="{DynamicResource Dashboard.NearestDeadlines}" Style="{StaticResource ReachItLabelTextStyle}" Margin="0,12,0,6" />
                        <ItemsControl ItemsSource="{Binding NearestDeadlines}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Title}" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <Button Command="{Binding NewTaskCommand}" Margin="0,14,0,0">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <iconPacks:PackIconMaterial Kind="PlusCircle" Width="16" Height="16" Margin="0,0,8,0" />
                                <TextBlock Text="{DynamicResource Dashboard.NewTask}" />
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding OpenActiveTasksCommand}"
                                Style="{StaticResource ReachItSecondaryButtonStyle}"
                                Margin="0,8,0,0"
                                ToolTip="{DynamicResource Dashboard.OpenActiveTasksTip}">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <iconPacks:PackIconMaterial Kind="FormatListBulleted" Width="16" Height="16" Margin="0,0,8,0" />
                                <TextBlock Text="{DynamicResource Dashboard.OpenActiveTasks}" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Border>

                <Border Grid.Row="2" Grid.Column="0" Style="{StaticResource ReachItCardStyle}">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Dashboard.FocusStatus}" Style="{StaticResource ReachItSectionTitleTextStyle}" />
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{DynamicResource Dashboard.Status}" Margin="0,0,4,0" />
                            <TextBlock Text="{Binding FocusStatus}" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                            <TextBlock Text="{DynamicResource Dashboard.Interruptions}" Margin="0,0,4,0" />
                            <TextBlock Text="{Binding InterruptionsCount}" />
                        </StackPanel>
                        <Button Command="{Binding ToggleFocusModeCommand}" Margin="0,14,0,0">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <iconPacks:PackIconMaterial Kind="PlayPause" Width="16" Height="16" Margin="0,0,8,0" />
                                <TextBlock Text="{DynamicResource Dashboard.ToggleFocus}" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Border>

                <Border Grid.Row="2" Grid.Column="2" Style="{StaticResource ReachItCardStyle}">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Dashboard.Productivity}" Style="{StaticResource ReachItSectionTitleTextStyle}" />
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{DynamicResource Dashboard.WorkedToday}" Margin="0,0,4,0" />
                            <TextBlock Text="{Binding WorkedTodayText}" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                            <TextBlock Text="{DynamicResource Dashboard.WorkedWeek}" Margin="0,0,4,0" />
                            <TextBlock Text="{Binding WorkedWeekText}" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                            <TextBlock Text="{DynamicResource Dashboard.Score}" Margin="0,0,4,0" />
                            <TextBlock Text="{Binding ProductivityScore}" />
                        </StackPanel>
                        <WrapPanel Margin="0,14,0,0">
                            <Button Command="{Binding OpenTaskCommand}" Style="{StaticResource ReachItSecondaryButtonStyle}" ToolTip="{DynamicResource Menu.Statistics}">
                                <StackPanel Orientation="Horizontal">
                                    <iconPacks:PackIconMaterial Kind="ChartLine" Width="16" Height="16" Margin="0,0,8,0" />
                                    <TextBlock Text="{DynamicResource Dashboard.MoreDetails}" />
                                </StackPanel>
                            </Button>
                            <Button Command="{Binding OpenProjectSettingsCommand}" Style="{StaticResource ReachItSecondaryButtonStyle}">
                                <StackPanel Orientation="Horizontal">
                                    <iconPacks:PackIconMaterial Kind="Cog" Width="16" Height="16" Margin="0,0,8,0" />
                                    <TextBlock Text="{DynamicResource Dashboard.Settings}" />
                                </StackPanel>
                            </Button>
                        </WrapPanel>
                    </StackPanel>
                </Border>
            </Grid>

            <Border Style="{StaticResource ReachItCardStyle}" Margin="0,14,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="280" />
                        <ColumnDefinition Width="14" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <iconPacks:PackIconMaterial Kind="Git" Width="24" Height="24" Margin="0,1,10,0" Foreground="{StaticResource SkyAquaBrush}" />
                            <TextBlock Text="{DynamicResource Tasks.VersionControl}" Style="{StaticResource ReachItSectionTitleTextStyle}" Margin="0" />
                        </StackPanel>
                        <TextBlock Text="{DynamicResource Tasks.VersionControlSubtitle}" Style="{StaticResource ReachItMutedTextStyle}" Margin="0,0,0,14" />

                        <Button Command="{Binding GitInitCommand}" ToolTip="{DynamicResource Tasks.GitInitTip}">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <iconPacks:PackIconMaterial Kind="SourceBranch" Width="16" Height="16" Margin="0,0,8,0" />
                                <TextBlock Text="{DynamicResource Tasks.GitInit}" />
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding OpenProjectFolderCommand}" Style="{StaticResource ReachItSecondaryButtonStyle}" ToolTip="{DynamicResource Tasks.OpenFolderTip}">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <iconPacks:PackIconMaterial Kind="FolderOpen" Width="16" Height="16" Margin="0,0,8,0" />
                                <TextBlock Text="{DynamicResource Tasks.OpenProjectFolder}" />
                            </StackPanel>
                        </Button>
                    </StackPanel>

                    <Grid Grid.Column="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <WrapPanel>
                            <Button Command="{Binding GitStatusCommand}" Style="{StaticResource ReachItSecondaryButtonStyle}" ToolTip="{DynamicResource Tasks.GitStatusTip}">
                                <StackPanel Orientation="Horizontal">
                                    <iconPacks:PackIconMaterial Kind="Refresh" Width="16" Height="16" Margin="0,0,8,0" />
                                    <TextBlock Text="{DynamicResource Tasks.GitStatus}" />
                                </StackPanel>
                            </Button>
                            <Button Command="{Binding GitStageAllCommand}" Style="{StaticResource ReachItSecondaryButtonStyle}" ToolTip="{DynamicResource Tasks.GitStageTip}">
                                <StackPanel Orientation="Horizontal">
                                    <iconPacks:PackIconMaterial Kind="PlusBoxMultiple" Width="16" Height="16" Margin="0,0,8,0" />
                                    <TextBlock Text="{DynamicResource Tasks.GitStageAll}" />
                                </StackPanel>
                            </Button>
                            <Button Command="{Binding GitCommitCommand}" Style="{StaticResource ReachItSecondaryButtonStyle}" ToolTip="{DynamicResource Tasks.GitCommitTip}">
                                <StackPanel Orientation="Horizontal">
                                    <iconPacks:PackIconMaterial Kind="CheckBold" Width="16" Height="16" Margin="0,0,8,0" />
                                    <TextBlock Text="{DynamicResource Tasks.GitCommit}" />
                                </StackPanel>
                            </Button>
                        </WrapPanel>

                        <Border Grid.Row="1"
                                Background="{StaticResource ReachItInputBrush}"
                                BorderBrush="{StaticResource ReachItBorderBrush}"
                                CornerRadius="8"
                                Padding="10"
                                Margin="0,6,0,0"
                                MinHeight="112">
                            <TextBlock Text="{Binding GitStatusText}" Style="{StaticResource ReachItMutedTextStyle}" FontFamily="Consolas" FontSize="12" TextWrapping="Wrap" />
                        </Border>
                    </Grid>
                </Grid>
            </Border>

            <Border Style="{StaticResource ReachItCardStyle}" Margin="0,14,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="14" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="14" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="{DynamicResource Dashboard.ProgressDashboard}" Style="{StaticResource ReachItSectionTitleTextStyle}" />
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{DynamicResource Dashboard.ActiveMilestone}" Margin="0,0,4,0" />
                            <TextBlock Text="{Binding ActiveMilestoneTitle}" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                            <TextBlock Text="{DynamicResource Dashboard.WorkItemsInProgress}" Margin="0,0,4,0" />
                            <TextBlock Text="{Binding WorkItemsInProgress}" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                            <TextBlock Text="{DynamicResource Dashboard.FilesChangedToday}" Margin="0,0,4,0" />
                            <TextBlock Text="{Binding FilesChangedToday}" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                            <TextBlock Text="{DynamicResource Dashboard.FocusMinutesToday}" Margin="0,0,4,0" />
                            <TextBlock Text="{Binding FocusMinutesToday, StringFormat={}{0:F0}}" />
                        </StackPanel>
                        <TextBlock Text="{DynamicResource Dashboard.CurrentContext}" Style="{StaticResource ReachItLabelTextStyle}" Margin="0,10,0,4" />
                        <TextBlock Text="{Binding CurrentWorkContext}" Style="{StaticResource ReachItMutedTextStyle}" />
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="{DynamicResource Dashboard.SuggestedTasks}" Style="{StaticResource ReachItSectionTitleTextStyle}" />
                        <ItemsControl ItemsSource="{Binding SuggestedTasks}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#101827" BorderBrush="{StaticResource ReachItBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="8" Margin="0,0,0,6">
                                        <StackPanel>
                                            <TextBlock Text="{Binding SuggestedTitle}" FontWeight="SemiBold" />
                                            <TextBlock Text="{Binding Reason}" Style="{StaticResource ReachItMutedTextStyle}" FontSize="11" />
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>

                    <Grid Grid.Column="4">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="12" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <StackPanel>
                            <TextBlock Text="{DynamicResource Dashboard.StaleTasks}" Style="{StaticResource ReachItSectionTitleTextStyle}" />
                            <ItemsControl ItemsSource="{Binding StaleWorkItems}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Title}" Style="{StaticResource ReachItMutedTextStyle}" Margin="0,0,0,4" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                        <StackPanel Grid.Row="2">
                            <TextBlock Text="{DynamicResource Dashboard.RecentlyCompleted}" Style="{StaticResource ReachItSectionTitleTextStyle}" />
                            <ItemsControl ItemsSource="{Binding RecentlyCompletedWorkItems}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Title}" Style="{StaticResource ReachItMutedTextStyle}" Margin="0,0,0,4" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
