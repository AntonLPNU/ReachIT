<!-- Task manager workspace content. -->
<UserControl x:Class="ReachIT.Presentation.Views.TaskManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks">
    <Grid Margin="0,0,0,14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="360" />
        </Grid.ColumnDefinitions>

        <Border Grid.Row="0" Grid.ColumnSpan="2" Style="{StaticResource ReachItHeroCardStyle}" Padding="18" Margin="0,0,0,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="FormatListBulleted" Width="28" Height="28" Margin="0,2,10,0" Foreground="White" />
                        <StackPanel>
                            <TextBlock Text="{DynamicResource Tasks.ManagerTitle}" Foreground="White" FontSize="25" FontWeight="Bold" />
                            <TextBlock Text="{DynamicResource Tasks.ManagerSubtitle}" Foreground="#EAF7FF" Margin="0,3,0,0" />
                        </StackPanel>
                    </StackPanel>
                </StackPanel>

                <WrapPanel Grid.Column="1" VerticalAlignment="Center">
                    <Border Background="#33101827" BorderBrush="#80FFFFFF" CornerRadius="8" Padding="12,8" Margin="8,0,0,0">
                        <StackPanel>
                            <TextBlock Text="{DynamicResource Tasks.Total}" Foreground="#DCEAFF" FontSize="11" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding Tasks.Count}" Foreground="White" FontSize="20" FontWeight="Bold" />
                        </StackPanel>
                    </Border>
                    <Border Background="#33101827" BorderBrush="#80FFFFFF" CornerRadius="8" Padding="12,8" Margin="8,0,0,0">
                        <StackPanel>
                            <TextBlock Text="{DynamicResource Tasks.DueTodayCount}" Foreground="#DCEAFF" FontSize="11" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding DueTodayCount}" Foreground="White" FontSize="20" FontWeight="Bold" />
                        </StackPanel>
                    </Border>
                    <Border Background="#33101827" BorderBrush="#80FFFFFF" CornerRadius="8" Padding="12,8" Margin="8,0,0,0">
                        <StackPanel>
                            <TextBlock Text="{DynamicResource Tasks.OverdueCount}" Foreground="#DCEAFF" FontSize="11" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding OverdueTasksCount}" Foreground="White" FontSize="20" FontWeight="Bold" />
                        </StackPanel>
                    </Border>
                </WrapPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Grid.ColumnSpan="2" Style="{StaticResource ReachItCardStyle}" Padding="14" Margin="0,0,0,14">
            <DockPanel LastChildFill="False">
                <WrapPanel DockPanel.Dock="Left" VerticalAlignment="Center">
                    <Button Command="{Binding AddTaskCommand}" ToolTip="Create a new task.">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="PlusCircle" Width="16" Height="16" Margin="0,0,8,0" />
                            <TextBlock Text="{DynamicResource Tasks.Add}" />
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding SaveAllCommand}" Style="{StaticResource ReachItSecondaryButtonStyle}" ToolTip="Save every task marked as unsaved.">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="ContentSave" Width="16" Height="16" Margin="0,0,8,0" />
                            <TextBlock Text="{DynamicResource Tasks.SaveAll}" />
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding DeleteTaskCommand}" Style="{StaticResource ReachItDangerButtonStyle}" ToolTip="Delete the selected task.">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="Delete" Width="16" Height="16" Margin="0,0,8,0" />
                            <TextBlock Text="{DynamicResource Tasks.DeleteSelected}" />
                        </StackPanel>
                    </Button>
                </WrapPanel>

                <WrapPanel DockPanel.Dock="Right" VerticalAlignment="Center">
                    <CheckBox Content="{DynamicResource Tasks.ActiveOnly}"
                              IsChecked="{Binding ShowActiveOnly}"
                              VerticalAlignment="Center"
                              Margin="0,0,14,6"
                              ToolTip="{DynamicResource Tasks.ActiveOnlyTip}" />

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <iconPacks:PackIconMaterial Kind="Magnify" Width="18" Height="18" Margin="0,8,7,0" Foreground="{StaticResource SkyAquaBrush}" />
                        <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                 Width="220"
                                 Margin="0,0,0,0"
                                 ToolTip="Filter tasks by title or description." />
                    </StackPanel>
                </WrapPanel>
            </DockPanel>
        </Border>

        <Border Grid.Row="2" Grid.Column="0" Style="{StaticResource ReachItCardStyle}" Padding="14" Margin="0,0,10,14">
            <StackPanel IsEnabled="{Binding IsTaskSelected}"
                        ToolTip="{DynamicResource Tasks.SelectTip}"
                        ToolTipService.ShowOnDisabled="True">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <iconPacks:PackIconMaterial Kind="ClipboardText" Width="22" Height="22" Margin="0,1,9,0" Foreground="{StaticResource SkyAquaBrush}" />
                    <TextBlock Text="{DynamicResource Tasks.Details}" Style="{StaticResource ReachItSectionTitleTextStyle}" Margin="0" />
                </StackPanel>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.35*" />
                        <ColumnDefinition Width="14" />
                        <ColumnDefinition Width="1.65*" />
                        <ColumnDefinition Width="14" />
                        <ColumnDefinition Width="0.65*" />
                        <ColumnDefinition Width="14" />
                        <ColumnDefinition Width="0.85*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Grid.Column="0" Grid.RowSpan="2">
                        <TextBlock Text="{DynamicResource Tasks.Title}" Style="{StaticResource ReachItLabelTextStyle}" />
                        <TextBox Text="{Binding EditTitle, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,0" ToolTip="Task title." />
                    </StackPanel>

                    <StackPanel Grid.Row="0" Grid.Column="2" Grid.RowSpan="2">
                        <TextBlock Text="{DynamicResource Tasks.Description}" Style="{StaticResource ReachItLabelTextStyle}" />
                        <TextBox Text="{Binding EditDescription, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" MinHeight="72" TextWrapping="Wrap" Margin="0,2,0,0" ToolTip="Notes or details for this task." />
                    </StackPanel>

                    <StackPanel Grid.Row="0" Grid.Column="4">
                        <TextBlock Text="{DynamicResource Tasks.PriorityShort}" Style="{StaticResource ReachItLabelTextStyle}" />
                        <TextBox Text="{Binding EditPriority, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,0" ToolTip="Priority from 1 to 3."/>
                    </StackPanel>

                    <StackPanel Grid.Row="0" Grid.Column="6">
                        <TextBlock Text="{DynamicResource Tasks.Status}" Style="{StaticResource ReachItLabelTextStyle}" />
                        <TextBox Text="{Binding EditStatus, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,0" ToolTip="Current task status, for example To Do or Done."/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="4" Grid.ColumnSpan="3" Margin="0,8,0,0">
                        <TextBlock Text="{DynamicResource Tasks.Deadline}" Style="{StaticResource ReachItLabelTextStyle}" />
                        <DockPanel LastChildFill="True">
                            <CheckBox DockPanel.Dock="Right" Content="{DynamicResource Tasks.Completed}" IsChecked="{Binding EditIsCompleted}" Margin="10,7,0,0" ToolTip="Mark this task as completed." />
                            <DatePicker SelectedDate="{Binding EditDeadlineDate}" Margin="0,2,0,0" ToolTip="Pick the task deadline." />
                        </DockPanel>
                    </StackPanel>
                </Grid>

                <DockPanel Margin="0,10,0,0" LastChildFill="False">
                    <WrapPanel DockPanel.Dock="Left">
                        <Button Content="{DynamicResource Tasks.Today}" Command="{Binding SetDeadlineTodayCommand}" Style="{StaticResource ReachItSecondaryButtonStyle}" ToolTip="Set deadline to today." />
                        <Button Content="{DynamicResource Tasks.Tomorrow}" Command="{Binding SetDeadlineTomorrowCommand}" Style="{StaticResource ReachItSecondaryButtonStyle}" ToolTip="Set deadline to tomorrow." />
                        <Button Content="{DynamicResource Tasks.Clear}" Command="{Binding ClearDeadlineCommand}" Style="{StaticResource ReachItSecondaryButtonStyle}" ToolTip="Remove the deadline." />
                    </WrapPanel>

                    <WrapPanel DockPanel.Dock="Right">
                        <Button Command="{Binding EditTaskCommand}" ToolTip="Apply edits to the selected task. Use Save All to write unsaved tasks.">
                            <StackPanel Orientation="Horizontal">
                                <iconPacks:PackIconMaterial Kind="CheckCircleOutline" Width="16" Height="16" Margin="0,0,8,0" />
                                <TextBlock Text="{DynamicResource Tasks.Apply}" />
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding AddSubtaskCommand}" Style="{StaticResource ReachItSecondaryButtonStyle}" ToolTip="Create a child task under the selected task.">
                            <StackPanel Orientation="Horizontal">
                                <iconPacks:PackIconMaterial Kind="FileTree" Width="16" Height="16" Margin="0,0,8,0" />
                                <TextBlock Text="{DynamicResource Tasks.AddSubtask}" />
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding MarkCompletedCommand}" Style="{StaticResource ReachItSecondaryButtonStyle}" ToolTip="Mark the selected task as completed.">
                            <StackPanel Orientation="Horizontal">
                                <iconPacks:PackIconMaterial Kind="Check" Width="16" Height="16" Margin="0,0,8,0" />
                                <TextBlock Text="{DynamicResource Tasks.MarkCompleted}" />
                            </StackPanel>
                        </Button>
                    </WrapPanel>
                </DockPanel>

                <TextBlock Text="{Binding DeadlineValidationMessage}" Foreground="{StaticResource NeonPinkBrush}" TextWrapping="Wrap" Margin="0,2,0,0"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="3" Grid.Column="0" Padding="0" Margin="0,0,10,0" CornerRadius="12" ClipToBounds="True">
        <Grid>
            <TextBlock Text="{DynamicResource Tasks.Empty}" Visibility="{Binding EmptyStateVisibility}" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource ReachItMutedTextStyle}" FontSize="16"/>

            <DataGrid ItemsSource="{Binding FilteredTasks}"
                      SelectedItem="{Binding SelectedTask}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      RowHeight="46"
                      VirtualizingStackPanel.IsVirtualizing="True"
                      VirtualizingStackPanel.VirtualizationMode="Recycling">
                <DataGrid.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Add Subtask" Command="{Binding AddSubtaskCommand}"/>
                        <MenuItem Header="Mark Completed" Command="{Binding MarkCompletedCommand}"/>
                        <MenuItem Header="Delete" Command="{Binding DeleteTaskCommand}"/>
                    </ContextMenu>
                </DataGrid.ContextMenu>
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="{DynamicResource Tasks.Title}" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <iconPacks:PackIconMaterial Kind="FileTree" Width="16" Height="16" Margin="0,0,8,0" Foreground="{StaticResource SkyAquaBrush}" />
                                    <TextBlock Text="{Binding DisplayTitle}" FontWeight="SemiBold" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="{DynamicResource Tasks.PriorityShort}" Binding="{Binding Priority}" Width="80" />
                    <DataGridTemplateColumn Header="{DynamicResource Tasks.Status}" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="#18243A" BorderBrush="#2D3C5C" CornerRadius="7" Padding="8,3" HorizontalAlignment="Left">
                                    <TextBlock Text="{Binding Status}" Foreground="{StaticResource SkyAquaBrush}" FontWeight="SemiBold" />
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridCheckBoxColumn Header="{DynamicResource Tasks.DoneShort}" Binding="{Binding IsCompleted}" Width="70" />
                    <DataGridTextColumn Header="{DynamicResource Tasks.Deadline}" Binding="{Binding DueDateUtc, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" Width="150" />
                    <DataGridCheckBoxColumn Header="{DynamicResource Tasks.Unsaved}" Binding="{Binding HasUnsavedChanges}" Width="90" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
        </Border>

        <Border Grid.Row="2" Grid.RowSpan="2" Grid.Column="1" Style="{StaticResource ReachItCardStyle}" Margin="10,0,0,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <iconPacks:PackIconMaterial Kind="Git" Width="24" Height="24" Margin="0,1,10,0" Foreground="{StaticResource SkyAquaBrush}" />
                        <TextBlock Text="{DynamicResource Tasks.VersionControl}" Style="{StaticResource ReachItSectionTitleTextStyle}" Margin="0" />
                    </StackPanel>

                    <TextBlock Text="{DynamicResource Tasks.VersionControlSubtitle}" Style="{StaticResource ReachItMutedTextStyle}" Margin="0,0,0,14" />

                    <Button Command="{Binding GitInitCommand}" ToolTip="{DynamicResource Tasks.GitInitTip}">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <iconPacks:PackIconMaterial Kind="SourceBranch" Width="16" Height="16" Margin="0,0,8,0" />
                            <TextBlock Text="{DynamicResource Tasks.GitInit}" />
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding GitStatusCommand}" Style="{StaticResource ReachItSecondaryButtonStyle}" ToolTip="{DynamicResource Tasks.GitStatusTip}">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <iconPacks:PackIconMaterial Kind="Refresh" Width="16" Height="16" Margin="0,0,8,0" />
                            <TextBlock Text="{DynamicResource Tasks.GitStatus}" />
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding GitStageAllCommand}" Style="{StaticResource ReachItSecondaryButtonStyle}" ToolTip="{DynamicResource Tasks.GitStageTip}">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <iconPacks:PackIconMaterial Kind="PlusBoxMultiple" Width="16" Height="16" Margin="0,0,8,0" />
                            <TextBlock Text="{DynamicResource Tasks.GitStageAll}" />
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding GitCommitCommand}" Style="{StaticResource ReachItSecondaryButtonStyle}" ToolTip="{DynamicResource Tasks.GitCommitTip}">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <iconPacks:PackIconMaterial Kind="CheckBold" Width="16" Height="16" Margin="0,0,8,0" />
                            <TextBlock Text="{DynamicResource Tasks.GitCommit}" />
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding OpenProjectFolderCommand}" Style="{StaticResource ReachItSecondaryButtonStyle}" ToolTip="{DynamicResource Tasks.OpenFolderTip}">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <iconPacks:PackIconMaterial Kind="FolderOpen" Width="16" Height="16" Margin="0,0,8,0" />
                            <TextBlock Text="{DynamicResource Tasks.OpenProjectFolder}" />
                        </StackPanel>
                    </Button>

                    <Separator Margin="0,10,0,12" />

                    <TextBlock Text="{DynamicResource Tasks.GitOutput}" Style="{StaticResource ReachItLabelTextStyle}" />
                    <Border Background="{StaticResource ReachItInputBrush}" BorderBrush="{StaticResource ReachItBorderBrush}" CornerRadius="8" Padding="10" Margin="0,6,0,0" MinHeight="170">
                        <TextBlock Text="{Binding GitStatusText}" Style="{StaticResource ReachItMutedTextStyle}" FontFamily="Consolas" FontSize="12" TextWrapping="Wrap" />
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
