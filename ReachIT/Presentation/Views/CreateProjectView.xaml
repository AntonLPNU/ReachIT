<!-- Create project form content. -->
<UserControl x:Class="ReachIT.Presentation.Views.CreateProjectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="140" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBlock Text="Project Name" Grid.Row="0" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center" />
        <TextBox Text="{Binding ProjectName, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,0,0,8" />

        <TextBlock Text="Description" Grid.Row="1" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center" />
        <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,0,0,8" />

        <TextBlock Text="Save Location" Grid.Row="2" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center" />
        <TextBox Text="{Binding SaveLocation, UpdateSourceTrigger=PropertyChanged}" Grid.Row="2" Grid.Column="1" Margin="0,0,8,8" />
        <Button Content="Browse" Command="{Binding BrowseLocationCommand}" Grid.Row="2" Grid.Column="2" Margin="0,0,0,8" />

        <TextBlock Text="Template" Grid.Row="3" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center" />
        <ComboBox Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2" ItemsSource="{Binding Templates}" SelectedItem="{Binding SelectedTemplate}" Margin="0,0,0,8" />

        <GroupBox Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="3" Header="Initial External Files" Margin="0,0,0,8">
            <DockPanel>
                <Button Content="Add initial external files" Command="{Binding AddInitialExternalFileCommand}" DockPanel.Dock="Top" Margin="0,0,0,8" />
                <ListBox ItemsSource="{Binding InitialExternalFiles}" />
            </DockPanel>
        </GroupBox>

        <StackPanel Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Create Project" Command="{Binding CreateCommand}" Margin="0,0,8,0" />
            <Button Content="Cancel" Command="{Binding CancelCommand}" />
        </StackPanel>
    </Grid>
</UserControl>
